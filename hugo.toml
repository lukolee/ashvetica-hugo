# baseURL = 'https://example.org/'
# locale = 'en-us'
# title = 'My New Hugo Project'
# theme = 'ananke'

baseURL = 'http://10.0.0.108:80'
noBuildLock = true
locale = 'en-au'
title = 'Ashvetica'
theme = "ASHVETICA-THEME"

[params]
  mainSections = ["projects"]
  # color style
  color = 'linen'                           # linen, wheat, gray, light
  email = 'contact@ashvetica.com'           # email for footer
  author = 'Ashley B'                       # author name for footer
  pageAuthor = "Luk Ole"                    # Web Developer
  pageAuthorLink = "https://lukole.au"      # Link to developer site

  # header social icons
  instagram = 'ashvetica'           # instagram.com/YOUR_INSTAGRAM_ID
  behance = 'ashvetica'                         # behance.net/YOUR_BEHANCE_ID
  # rss = true                                # show rss icon

  # home page profile
  avatar = 'GRAVATAR_EMAIL'                 # gravatar email or image url
  name = 'Ashley B'
  bio = 'BIOGRAPHY'

# misc
disableHLJS = true                        # disable highlight.js
disablePostNavigation = true              # disable post navigation
monoDarkIcon = true                       # show monochrome dark mode icon
gravatarCdn = 'GRAVATAR_CDN_LINK'         # e.g. 'https://cdn.v2ex.com/gravatar/'
math = true                               # enable KaTeX math typesetting globally
localKatex = false                        # use local KaTeX js/css instead of CDN
graphCommentId = "YOUR_GRAPH_COMMENT_ID"  # use graph comment (disqus alternative)
favicon = "favicon.ico"                   # customize the default favicon
appleTouchIcon = "apple-touch-icon.png"   # customize the default Apple touch icon
